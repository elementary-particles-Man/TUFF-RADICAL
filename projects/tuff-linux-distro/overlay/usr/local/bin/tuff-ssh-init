#!/bin/bash
# TUFF-RADICAL: Early SSH Bootstrapping

# Restart SSH asynchronously without freezing system initialization
(
    systemctl restart sshd || true
) > /dev/null 2>&1 &

# Immediately detach and let systemd continue the boot chain
disown
exit 0
