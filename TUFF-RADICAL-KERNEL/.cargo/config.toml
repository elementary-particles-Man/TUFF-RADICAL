[build]
target = "x86_64-unknown-uefi"

[target.x86_64-unknown-uefi]
# The kernel does runtime SIMD probing, so the build should stay on a generic
# x86_64 baseline instead of inheriting host-specific AVX tuning.
rustflags = [
    "-C", "target-cpu=x86-64",
]
